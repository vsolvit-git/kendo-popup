pages:
  cache:
    paths:
    - node_modules/

  stage: deploy
  script:
  - npm install -g @angular/cli@8.2.14
  - npm install
  - ng build
  - mv dist/kendo-popup/* public/
  artifacts:
    paths:
    - public
  only:
  - master